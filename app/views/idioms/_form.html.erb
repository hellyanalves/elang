<%= form_for(@idiom) do |f| %>
  <% if @idiom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@idiom.errors.count, "error") %> prohibited this idiom from being saved:</h2>

      <ul>
      <% @idiom.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :Country_id %><br>
    <%= f.text_field :Country_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
